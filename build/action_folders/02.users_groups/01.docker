#!/bin/bash

declare -A docker=(
    ['user']=${DOCKER_USER:-docker}
    ['uid']=${DOCKER_UID:-$(docker.UID)}
    ['group']=${DOCKER_GROUP:-docker}
    ['gid']=${DOCKER_GID:-$(docker.GID)}
    ['shell']=/bin/bash
)
