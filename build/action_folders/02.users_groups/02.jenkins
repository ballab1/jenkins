#!/bin/bash

declare -A jenkins=(
    ['user']=${JENKINS_USER:-jenkins}
    ['uid']=${JENKINS_UID:-$(jenkins.UID)}
    ['group']=${JENKINS_GROUP:-jenkins}
    ['gid']=${JENKINS_GID:-$(jenkins.GID)}
    ['home']="${JENKINS_HOME:-/var/jenkins_home}"
    ['shell']=/bin/bash
)
