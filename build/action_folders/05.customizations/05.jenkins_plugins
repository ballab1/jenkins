#!/bin/bash

declare -r pluginsFile=/usr/share/jenkins/ref/plugins.txt
declare -a plugins

readarray -t plugins < "$pluginsFile"

# download plugins and save a copy to restore at each runtime
chmod 775 /usr/local/bin/install-plugins.sh
/usr/local/bin/install-plugins.sh "${plugins[@]}"
